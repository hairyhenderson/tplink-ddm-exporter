version: '3.8'

services:
  tplink-ddm-exporter:
    image: tplink-ddm-exporter:latest
    container_name: tplink-ddm-exporter
    network_mode: host
    environment:
      - SNMP_TARGET=192.168.1.1
      - SNMP_COMMUNITY=public
      - LISTEN_ADDR=:9116
    restart: unless-stopped

